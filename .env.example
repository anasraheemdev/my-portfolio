# Groq (portfolio chatbot)
# Create a file named `.env.local` in the project root (gitignored) and set:
GROQ_API_KEY=
# Optional — defaults to llama-3.3-70b-versatile
# GROQ_MODEL=llama-3.1-8b-instant
